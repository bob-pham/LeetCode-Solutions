// @leet start
function coinChange(coins: number[], amount: number): number {
	let mem: number[] = [0];
	let max_coin = -1;

	for (let i = 0; i <= amount; i++) {
		
		mem.push(0);
	}
    
};
// @leet end
